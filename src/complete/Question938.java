package complete;

// Attempt 1 Completed -> 05/21/2022
public class Question938 {

    /*
    public int rangeSumBST(TreeNode root, int low, int high) {
        if (root == null)
            return 0;
        int amount = root.val >= low && root.val <= high ? root.val : 0;
        return amount + rangeSumBST(root.left, low, high) + rangeSumBST(root.right, low, high);
    }
     */

}
